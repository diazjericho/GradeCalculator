<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.screens.main.settings.backup_restore.BackupRestoreScreen">

    <LinearLayout
        android:id="@+id/layout_backup_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">
        <Button
            android:id="@+id/button_settings_backup"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="Backup database"/>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/layout_restore_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp"
        app:layout_constraintTop_toBottomOf="@+id/layout_backup_button">

        <Button
            android:id="@+id/button_settings_restore"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="Restore database" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>